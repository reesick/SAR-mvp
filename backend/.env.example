# ─── Database ───────────────────────────────────────
DATABASE_URL=postgresql://postgres:postgres123@localhost:5432/aml_system

# ─── LLM Provider Toggle ───────────────────────────
# Set to "yes" to use Groq Cloud API (free, fast, needs internet)
# Set to "no" to use local Ollama (offline, needs GPU)
GROQ_ENABLED=no

# ─── Groq (only needed if GROQ_ENABLED=yes) ────────
# Get your free API key at: https://console.groq.com/keys
GROQ_API_KEY=gsk_your_key_here

# ─── Ollama (only needed if GROQ_ENABLED=no) ───────
OLLAMA_URL=http://localhost:11434

# ─── Auth / JWT ─────────────────────────────────────
SECRET_KEY=change-this-to-a-random-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
